services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: lbf
      POSTGRES_PASSWORD: lbf
      POSTGRES_DB: lbf
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
