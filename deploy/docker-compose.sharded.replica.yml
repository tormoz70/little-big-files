# Override for sharded stand with replica + segment sync (use with --profile replica).
services:
  coordinator:
    volumes:
      - ../deploy/shards.bootstrap.replica.json:/app/deploy/shards.bootstrap.json:ro
